import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/Header'
import Footer from '@/components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Cedar Grove Ambulance & Rescue Squad',
  description: 'Professional emergency medical services serving Cedar Grove, NJ since 1938. Neighbors helping neighbors with compassionate, life-saving care.',
  keywords: 'Cedar Grove EMS, ambulance, rescue squad, emergency medical services, New Jersey, volunteer EMT',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}